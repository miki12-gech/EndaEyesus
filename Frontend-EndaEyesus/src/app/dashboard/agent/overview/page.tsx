import { OverviewTab } from "@/features/agent/overview-tab";

export const metadata = {
  title: "Overview - Admin",
  description: "Platform overview dashboard",
};

export default function OverviewPage() {
  return <OverviewTab />;
}